{
  "name": "cloudmind",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Host-run AI agent runtime with Docker as a tool surface.",
  "engines": {
    "node": ">=24.10.0"
  },
  "scripts": {
    "test": "node --test"
  }
}
