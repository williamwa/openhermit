# Memory Model Docs

Memory documentation is now split by architecture version.

## Documents

- `v1` memory model: [docs/v1/memory-model.md](v1/memory-model.md)
- `v2` memory direction: [docs/v2/architecture.md](v2/architecture.md) and [docs/v2/plan.md](v2/plan.md)

## Notes

`v1` describes the current file-based memory layout inside the workspace.

`v2` will redefine memory as program-managed state rather than agent-authored workspace content.
